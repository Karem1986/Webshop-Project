//feed reducer
const initialState = []//initially we dont have any products 
console.log("Hi feed reducer!")

export default function productFeed(state = initialState, action = {}) {
    return state;
}

